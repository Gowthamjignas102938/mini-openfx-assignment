DATABASE_URL=postgresql://postgres:postgres@localhost:5434/miniopenfx
REDIS_URL=redis://localhost:6381

# Shared secret required on every /v1/* request via the X-API-Key header.
# This placeholder is fine for local dev; generate a real one for anything
# else, e.g.: openssl rand -hex 32
API_KEY=local-dev-api-key
