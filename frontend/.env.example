VITE_API_BASE_URL=http://localhost:3000/v1

# Must match the backend's API_KEY (see the root .env.example). Baked into
# the built JS bundle at build time — not a real secret once this ships to
# a browser, see README's "Design decisions & trade-offs".
VITE_API_KEY=local-dev-api-key
